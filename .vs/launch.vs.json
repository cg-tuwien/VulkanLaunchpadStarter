{
  "version": "0.2.1",
  "defaults": {},
  "configurations": [
    {
        "type": "default",
        "project": "CMakeLists.txt",
        "projectTarget": "VulkanLaunchpadStarter.exe",
        "name": "VulkanLaunchpadStarter.exe (in workspaceRoot)",
        "currentDir": "${workspaceRoot}"
    }
  ]
}
